extends layout

block content
    .inner 
        h2 #{title} (#{tag})
        ul.tags
            each t in tags
                li.tag
                    a.tag__link(href=`/tags/${t._id}`)
                        span.tag__text=  t._id
                        span.tag__count=  t.count